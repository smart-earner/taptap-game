# 首批正式编码状态｜core-0.1

日期：2026-09-19。架构已先提交，当前文件记录随后源码交付。PRD不变，应用完成度不由文档页数代替。

## 已写入代码并运行验证

| 能力 | 本批实际边界 |
| --- | --- |
| 领域模型 | 唯一国库、分城库存、五维属性、职业经验、城市／辖区／岗位关联 |
| 命令事务 | 权限／revision校验、同ID重试去重、失败回滚、持久化后发布状态 |
| 分层任职 | 主公建立测试辖区，都督在授权池内选择同城太守；锁定和一人一职 |
| 太守调岗 | 按安民／兴商／兴业等方针在现有岗位容量内分工，粮食底线优先 |
| 经济切片 | 600秒原子批次、整数千分资源、居民口粮余数、预留保护；无建造队列 |
| 人物成长 | 在岗有效工作累积职业XP；都督不按城市数量倍增；不刷任免经验 |
| 时间推进 | 分段／整段一致；倒退不倒扣；七天正常上限、重复返回不重复收益 |
| 存档 | 8MB上限、版本与完整性校验、三个备份、原子写入；损坏不自动建新档 |
| 命令行 | 一城正常样例／三城演示；方针对比；可选显式路径存档 |
| Mac代码 | SwiftUI主公府与城市概览、菜单栏、退出前保存、唤醒刷新；未做SDK编译 |

正常开局目前只有代理太守和基础采集，尚不能在UI里扩大设施。三城演示预置岗位只是测试夹具，不是完整G2，不含运输、财政周期和军团。界面有明确未实现提示，不伪装成完整游戏。

## 已执行的检查

- Swift工具链：6.2.1；平台：x86_64-unknown-linux-gnu。
- `swift build --product SanguoCLI`：通过。
- `swift test`：40项Swift Testing测试通过。
- `swift test -c release`：40项通过（同一套测试的Release复验，不说成80项不同测试）。
- 命令行6小时兴商／兴业演示：通过，产生不同的真实资源结果。
- `swiftc -frontend -parse Sources/SanguoMac/SanguoMacApp.swift`：语法解析通过，不解析Apple SDK类型。
- `bash -n scripts/build-macos.sh`：Shell语法通过；没有实际构建.app。
- 原`python3 scripts/validate_spec.py`：57项历史静态检查通过。

测试覆盖：权限越界、重复任职、人才池与锁定、跨城瞬移拒绝、过期命令、并发同ID去重、预算预留／消费／释放、预留粮保护、方针产出差异、时钟边界、在岗经验、损坏拒绝、备份与写失败不提交。

## 未执行／未实现

Mac SDK类型检查、macOS构建、M4窗口与能耗、签名公证、像素美术、输入监控和行情接入均未验证。原100项应用验收没有因此标记通过。

仍待实现：批次开始预留与精确项目事件；建造升级；周期财政和父子额度联动；跨城调任／物流；军团与战斗；常驻收藏路径；培养树、研习和装备效果；十项决策卡；SpriteKit城景。

职业经验是可运行起点，不能代替完整属性平衡。600秒批次切片尚不覆盖中途变更生产策略的精确连续工时分摊；后续项目化时单独验收。主档损坏时当前UI显示错误和恢复说明，图形备份选择器尚未制作。

## 下一批应做的开发

优先完成一城的建设项目与材料预留，把当前“会调岗、会生产”接到“自主发展新产业”，再增加可配置的培养项目和职业分支。之后才接三城物资运输、父子财政与跨城人事。不要先接行情、采集输入或大模型。
