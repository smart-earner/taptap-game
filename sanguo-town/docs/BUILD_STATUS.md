# 当前交付状态｜desktop-0.1 / town-0.5

2026-09-20。本轮只增加桌面表现和窗口承载，经营规则与存档schema不变。原town-0.5功能和限制见CITY_IDENTITY.md及identity-test-report.json；完整游戏仍在开发。

## 已实现

- 公开AppKit桌面层NSPanel：无标题栏、透明、不可成为key/main窗口、鼠标穿透、不隐藏于应用失焦。
- 同一AppModel快照驱动独立显示面，不创建第二个GameSession或模拟心跳。
- 保留普通城景和主公府，菜单栏切换、隐藏与暂停；显示设置支持屏幕、城市、大小和位置。
- 单屏优先、断屏回退、负原点屏幕定位、重连原屏；显示偏好单独持久化，损坏时回退关闭。
- 透明SpriteKit与移除整块天空背景；保留真实建筑/道路/居民，桌面隐藏名字与等级文字。
- 桌面12fps、低电量6fps；手动暂停、减少动态效果、应用隐藏与睡眠/会话停用暂停。

## 已验证

被测源码`abaaed75c66861dcb4542a242f312d330439eb95`。
[原生CI 35516594162](https://github.com/smart-earner/taptap-game/actions/runs/35516594162)通过：macOS15.7.9 arm64、Xcode16.4、Swift6.1.2；Debug201项、Release同套201项；45个30/60/90日经营检查点；SwiftUI/SpriteKit编译及开发包构建。新增23项=16跨平台显示+7原生窗口对象，既有178保留。Linux Swift6.2.1 Release194项通过，未执行7项AppKit测试。

浏览器预览由同一Swift绘图和人物帧导出，Chromium内存加载验证阶段切换、暂停、模拟遮挡、大小与收起；无JavaScript错误或外部网络。导出的SVG四角alpha为0。环境禁止file URL导航，未把set_content检验说成Safari本地打开验收。

## 不能声称通过的部分

原生面板对象属性测试不等于真实Finder点击、桌面图标拖拽、所有壁纸层叠加、Mission Control、全屏、台前调度、外接屏热插拔或M4长时能耗验收。浏览器背景/文件/窗口均为示意，不是实机桌面截图。

Finder透明图标画布可能让occlusionState保守，所以桌面模式不以该信号单独暂停；这会牺牲完全遮挡时的一部分省电效果。未读其他应用窗口作为补偿。用户可手动暂停/隐藏；普通窗口模式仍使用遮挡暂停。

没有更换壁纸，没有启用系统登录项，没有安装系统扩展。ad-hoc开发包未Developer ID公证。全部PRD、长期玩法和发行验收仍未完成。

## 分批上传

第一批3b50c6d：只读城景与位置规则＋16测试；第二批abaaed7：原生托管、设置、客户端接入＋7原生测试；最后补充本页与需求增补，未改被测运行源。CI保持contents:read，没有临时自动改写源码的工作流。
