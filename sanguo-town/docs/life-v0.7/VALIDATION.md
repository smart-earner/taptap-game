# PRD v0.7.0 规格验证与交付记录

**交付为完整研发规格，不是life-0.7运行版本。** 规范基线日期2026-09-20，验证时间与平台以链接CI日志为准。代码仍town-0.5＋desktop-0.1；本轮没有修改Sources、Tests或Package.swift。

## 1. 完整组成

主入口＋十章规范、三份JSON、两份可重复执行的规格检查与阅读导出脚本。参数覆盖资源/配方/作物、20工种、食品与服务、十类建筑、八街区、四条三阶段特色工程、六将/六器/四马/木印、12常驻事件、三城与真实安置、全桌面生活、迁移和72项应用验收。

新开局的16个身份、床位、职责、库位和饭食来源另有bootstrap JSON，不能只根据总数自行摆资源。历史PRD v0.6入口按原Git blob保存，数值和规则不并行作用于新订单。

## 2. 已实际执行的检查

最终数值/导出脚本验证提交：`9fd5355df72dadbf3ce0e451fbf9ec34cbf457d3`。

[只读规格CI run35552893315](https://github.com/smart-earner/taptap-game/actions/runs/35552893315)成功。取回归档后在本地Python重跑两份脚本，检查列表和结果与CI一致。

|检查|实际结果|不能代表什么|
|---|---:|---|
|基础结构、引用、数值向量与独立容量参照|519 PASS，0 FAIL|不是完整Swift生活模拟|
|开局分库、床位、条件链接、配置驱动容量与全文导出|54 PASS，0 FAIL|不是所有玩法已经平衡|
|六种错误配置反向测试，两脚本完整流程|6种全部拒绝|不是新增6项游戏验收|
|应用验收定义|72项，全部NOT_RUN|不能用静态PASS代替游戏通过|
|M4桌面交互/能耗、新生活版长期试玩|NOT_RUN|不沿用旧客户端的201项测试|

反向测试包括：田边缓冲过小、制粮席不足、林地树数过少、无效工种引用、错误初始人数、作物成熟时间不匹配。过程中发现第一份脚本的林地参照使用固定基线数，最终第二份交付校验改为读取实际树数、产量、再生/劳动时间、人口与食谱，12棵树的错误配置现在会失败。两份脚本必须按README顺序运行，不能只运行第一份便宣称完成交付核验。

产物artifact ID `10618469961`；归档SHA-256 `ec0a8f072a040eebae56f1e4fe9d3852a67cc39ac4530dcd17d3a1eb9cd0bd87`。CI产物有保留期限，正文、配置、脚本留在Git，可重新导出。

## 3. 容量参照的解释

12组人口/军团需求是算术矩阵：居民16/64/150×军团0/30/60/90。最大夹具全家常饭＋素口粮每天需2925份食粮、731.25份原木作为食品原料/燃料。程序还给出单田床/单设备、无共享工人和无运输争用时的作息参照，帮助排除明显不可能。

这些**不是一整座城已经稳定供应150居民和90士兵的证据**。工人兼岗、补水、运输、保供切换、军需、施工与菜品同时运行，仍须真正实现V1—V5后做端到端供需测试。30/60/90日45点矩阵在本轮是验收计划，不是新生活引擎的已通过结果。

## 4. 阅读版

完整Markdown含主入口、十章和全部三份JSON；HTML使用相同文本导出，包含目录、表格与可阅读技术字段。Linux Chromium通过set_content检查桌面宽度、六将表、特色工程表和窄屏换行，无页面脚本错误或自动外部请求；不是Mac游戏界面或Safari本地文件权限测试。

## 5. 原生CI与历史规格隔离

旧v0.6规格工作流改为手动检出其原始提交，避免用新PRD入口验证旧参数。当前Mac工作流先运行两份新规格检查，再回归既有Swift代码及town-0.5矩阵；运行结果分别标注。只读工作流不会改写源码或自动合并主分支。

[Mac流程调整后的运行记录](https://github.com/smart-earner/taptap-game/actions/runs/35552966375)对应既有代码，不把其结果算作life-0.7完成度。本页以后续具体运行状态为准，不在规格交付时预先给未完成步骤填PASS。

## 6. 再生成

```sh
cd sanguo-town
python3 scripts/validate_prd_v07.py --output dist/prd-v07
python3 scripts/check_prd_v07_delivery.py --output dist/prd-v07
```

输出validation.json、delivery-validation.json、capacity-reference.json、acceptance-plan.json、PRD-v0.7-complete.md与PRD-v0.7-readable.html。所有新应用用例保持NOT_RUN。数值明确是为了开发可执行与结果可核验，不等于已经证明游戏完整或长期好玩。
