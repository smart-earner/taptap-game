# 第三批：经过校验的核心集成补丁

基底是第二批固定的 `town-0.4-integration/Sources` 树。core-fixes.patch 补齐存档、命令路由、统一预留校验、在途人事保护、开局市场资金保护与地区快照。RealmTests.swift 新增24项回归。

本地 Linux Swift 6.2.1 干净构建139项测试通过；此前被中断的增量编译出现过测试进程崩溃，独立scratch目录重新完整构建后通过，不能将旧增量结果算为成功。

本批专用工作流只在指定开发分支执行：复制固定检查点、校验并应用补丁、运行完整Mac Debug/Release测试及客户端编译；全部通过后才生成普通源码提交。推送前检查分支未移动，只允许提交SanguoCore与新增测试，不修改main、不强推、不接收PR输入。结果以工作流日志和verified-commit.txt为准；补丁已上传不等于集成已通过。
